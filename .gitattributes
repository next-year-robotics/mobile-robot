# STM32CubeMX emits CRLF and mixed-EOL generated files. Treat the terminal CR
# as part of the line ending while retaining the other whitespace checks.
firmware/base_controller/** whitespace=cr-at-eol
